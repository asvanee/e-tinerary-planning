/**
 * Manual overrides สำหรับ dedupe
 * ------------------------------------------------------------------
 * ใช้สำหรับเคสที่อัลกอริทึมอัตโนมัติ (dedupe.ts) มองไม่ออกโดยธรรมชาติ เช่น:
 *   - ชื่อคนละภาษากัน (ไทย vs อังกฤษ) เทียบ string similarity ไม่ได้เลย
 *   - ATT_LOCATION พังทั้งคู่ (เช่น Google Maps short link) เช็ค proximity ไม่ได้
 * รายการที่ใส่ในไฟล์นี้ต้องผ่านการตรวจสอบโดยคน (ไม่ใช่เดา) ก่อนใส่เสมอ
 * ใส่ comment กำกับที่มาของการตัดสินใจทุกรายการ เพื่อ audit ย้อนหลังได้
 */

export interface LocationOverride {
  lat: number;
  lng: number;
  source: string; // อธิบายว่าไปเอาพิกัดมาจากไหน / ใครยืนยัน
}

/**
 * แก้พิกัดที่ผิด/ใช้ไม่ได้ใน ATT_LOCATION เดิม โดย key คือ ATT_ID
 * ใส่พิกัดจริงจาก Google Maps ที่เปิดหมุดแล้ว copy lat,lng มาโดยตรง
 * (อย่าเดา ไม่งั้นข้อมูลจะผิดกว่าการปล่อยว่างไว้อีก)
 *
 * TODO: เปิด Google Maps หาที่ตั้งจริงของ Tiffany's Show Pattaya
 * (464 หมู่ 9 ถนนพัทยาสาย 2 ต.หนองปรือ อ.บางละมุง จ.ชลบุรี 20150)
 * แล้วแทนค่า lat/lng ด้านล่างนี้ก่อนใช้งานจริง — ตอนนี้ปล่อยว่างไว้ตั้งใจ
 * เพราะไม่อยากใส่พิกัดที่ไม่ได้ verify เอง
 */
export const locationOverrides: Record<string, LocationOverride> = {
  // "20241220131515001": { lat: 0, lng: 0, source: "TODO: กรอกพิกัดจริงจาก Google Maps" },
  // "20241220131453001": { lat: 0, lng: 0, source: "TODO: กรอกพิกัดจริงจาก Google Maps" },
  // "20241220130749001": { lat: 0, lng: 0, source: "TODO: กรอกพิกัดจริงจาก Google Maps" },
  // "20100518224629675": { lat: 0, lng: 0, source: "TODO: กรอกพิกัดจริงจาก Google Maps" },
};

/**
 * กลุ่ม ATT_ID ที่คนยืนยันแล้วว่าเป็นสถานที่เดียวกันจริง แม้อัลกอริทึมจะแยกไว้
 * แต่ละ group คือ array ของ ATT_ID ที่ต้อง merge เป็นแถวเดียวกัน
 */
export const forceMergeGroups: string[][] = [
  [
    // Tiffany's Show Pattaya — คาบาเรต์โชว์ชื่อดังพัทยา ยืนยันโดยคนแล้วว่าเป็นที่เดียวกันจริง
    // อัลกอริทึมจับไม่ได้เพราะ (1) ATT_LOCATION เป็น Google Maps short link พังทั้ง 4 แถว
    // เช็ค proximity ไม่ได้ (2) ชื่อคนละภาษา (ไทย/อังกฤษ) เทียบ string similarity ไม่ได้
    "20100518224629675", // ทิฟฟานี โชว์ (ตัวแทนหลัก — เลือกเพราะ ATT_ID เก่าสุด ดูน่าจะเป็น record ต้นทาง)
    "20241220131515001", // ทิฟฟานี่โชว์ พัทยา
    "20241220131453001", // Tiffany's Show Pattaya
    "20241220130749001", // ทิฟฟานี่โชว์ พัทยา
  ],
];